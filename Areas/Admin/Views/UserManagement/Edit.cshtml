@model UserEditVM
@{
    ViewData["Title"] = "Edit User";
}

<form asp-area="Admin" asp-controller="UserManagement" asp-action="Edit" method="post">
    @Html.AntiForgeryToken()
    <input type="hidden" asp-for="Id" />
    <div class="form-grid">
        <div class="form-group">
            <label asp-for="FirstName"></label>
            <input asp-for="FirstName" class="form-control" />
            <span asp-validation-for="FirstName"></span>
        </div>
        <div class="form-group">
            <label asp-for="LastName"></label>
            <input asp-for="LastName" class="form-control" />
            <span asp-validation-for="LastName"></span>
        </div>
        <div class="form-group">
            <label asp-for="UserName"></label>
            <input asp-for="UserName" class="form-control" />
            <span asp-validation-for="UserName"></span>
        </div>
        <div class="form-group">
            <label asp-for="Email"></label>
            <input asp-for="Email" class="form-control" />
            <span asp-validation-for="Email"></span>
        </div>
    </div>
    <div class="form-group">
        <label>Roles</label>
        <div class="role-checkbox-list">
            @if (Model.AllRoles != null)
            {
                foreach (var role in Model.AllRoles)
                {
                    var isChecked = Model.SelectedRoles != null && Model.SelectedRoles.Contains(role);
                    <label class="role-checkbox">
                        <input type="checkbox" name="SelectedRoles" value="@role" checked="@isChecked" />
                        @role
                    </label>
                }
            }
        </div>
    </div>
    <div class="form-actions">
        <button type="submit" class="btn-primary">Save Changes</button>
        <a asp-action="ChangePassword" asp-route-id="@Model.Id" class="btn-secondary">Change Password</a>
        <a asp-action="Index" class="btn-secondary">Cancel</a>
    </div>
</form>